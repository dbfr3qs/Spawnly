---
page_title: "Spawnly Provider"
description: |-
  Manage Spawnly agent templates in the registry control plane — config-as-code for the agent catalog.
---

# Spawnly Provider

The Spawnly provider manages **agent templates** in the Spawnly registry's
control plane: the spawnable definitions of each agent type, as code.

It talks directly to the registry control-plane API using a shared-secret
bearer token.

## Example Usage

```terraform
terraform {
  required_providers {
    spawnly = {
      source = "registry.terraform.io/spawnly/spawnly"
    }
  }
}

provider "spawnly" {
  endpoint = "http://localhost:18080" # port-forwarded registry
  # token sourced from SPAWNLY_TOKEN (the bootstrap-generated control-plane secret)
}

resource "spawnly_agent_template" "demo" {
  agent_type      = "tf-demo-worker"
  version         = "1.0.0"
  status          = "active"
  requires_tenant = false
  oauth_scopes    = ["sample-api-a:write"]

  meta {
    display_name = "TF Demo Worker"
    description  = "Created by the Spawnly Terraform provider"
  }

  runtime_spec {
    image         = "agent-chain-worker:latest"
    lifecycle     = "short-lived"
    supports_chat = false

    env_defaults = {
      SAMPLE_API_URL = "http://sample-api-global"
      LOG_LEVEL      = "info"
    }

    resources {
      cpu_limits    = "500m"
      memory_limits = "256Mi"
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `endpoint` (String) Base URL of the registry control-plane API. May also be set via the `SPAWNLY_ENDPOINT` environment variable.
- `token` (String, Sensitive) Shared-secret bearer token for the control-plane API. May also be set via the `SPAWNLY_TOKEN` environment variable. Leave unset against a registry running open (`CONTROL_PLANE_AUTH=none`).
