import { a as InMemoryFs, i as createMemoryStateBackend, n as createWorkspaceStateBackend, r as FileSystemStateBackend, s as StateBatchOperationError, t as WorkspaceFileSystem } from "./workspace-C2c-02Ga.js";
import { n as STATE_TYPES, t as STATE_SYSTEM_PROMPT } from "./prompt-DOQjhIfY.js";
import { channel } from "node:diagnostics_channel";
//#region src/filesystem.ts
function isSqlStorage(src) {
	return typeof src === "object" && src !== null && "databaseSize" in src;
}
function isD1Database(src) {
	return typeof src === "object" && src !== null && "prepare" in src && "batch" in src;
}
function toBackend(src) {
	if (isSqlStorage(src)) {
		const storage = src;
		return {
			query(sql, ...params) {
				return [...storage.exec(sql, ...params)];
			},
			run(sql, ...params) {
				storage.exec(sql, ...params);
			}
		};
	}
	if (isD1Database(src)) {
		const db = src;
		return {
			async query(sql, ...params) {
				return (await db.prepare(sql).bind(...params).all()).results;
			},
			async run(sql, ...params) {
				await db.prepare(sql).bind(...params).run();
			}
		};
	}
	return src;
}
const DEFAULT_INLINE_THRESHOLD = 15e5;
const TEXT_ENCODER = new TextEncoder();
const TEXT_DECODER = new TextDecoder();
const MAX_SYMLINK_DEPTH = 40;
const VALID_NAMESPACE = /^[a-zA-Z][a-zA-Z0-9_]*$/;
const LIKE_ESCAPE = "\\";
const MAX_STREAM_SIZE = 100 * 1024 * 1024;
const MAX_DIFF_LINES = 1e4;
const MAX_PATH_LENGTH = 4096;
const MAX_SYMLINK_TARGET_LENGTH = 4096;
const MAX_MKDIR_DEPTH = 100;
const workspaceRegistry = /* @__PURE__ */ new WeakMap();
function describeR2(r2) {
	return r2 === null ? "none" : "R2 bucket";
}
const wsChannel = channel("agents:workspace");
var Workspace = class {
	constructor(options) {
		this.initialized = false;
		const { sql: source } = options;
		const ns = options.namespace ?? "default";
		if (!VALID_NAMESPACE.test(ns)) throw new Error(`Invalid workspace namespace "${ns}": must start with a letter and contain only alphanumeric characters or underscores`);
		const nextConfig = {
			r2: options.r2 ?? null,
			r2Prefix: options.r2Prefix,
			inlineThreshold: options.inlineThreshold ?? DEFAULT_INLINE_THRESHOLD
		};
		const registered = workspaceRegistry.get(source) ?? /* @__PURE__ */ new Map();
		const prevConfig = registered.get(ns);
		if (prevConfig) {
			const diffs = [];
			if (prevConfig.r2 !== nextConfig.r2) diffs.push(`r2 (previous=${describeR2(prevConfig.r2)}, new=${describeR2(nextConfig.r2)})`);
			if (prevConfig.r2Prefix !== nextConfig.r2Prefix) diffs.push(`r2Prefix (previous=${JSON.stringify(prevConfig.r2Prefix)}, new=${JSON.stringify(nextConfig.r2Prefix)})`);
			if (prevConfig.inlineThreshold !== nextConfig.inlineThreshold) diffs.push(`inlineThreshold (previous=${prevConfig.inlineThreshold}, new=${nextConfig.inlineThreshold})`);
			if (diffs.length > 0) throw new Error(`Workspace "${ns}" on this sql was previously constructed with different options: ${diffs.join(", ")}. Two Workspaces sharing the same storage and namespace must use identical r2, r2Prefix, and inlineThreshold or reads of large files will fail.`);
		} else {
			registered.set(ns, nextConfig);
			workspaceRegistry.set(source, registered);
		}
		this.sql = toBackend(source);
		this._nameOrFn = options.name;
		this.namespace = ns;
		this.tableName = `cf_workspace_${ns}`;
		this.indexName = `cf_workspace_${ns}_parent`;
		this.r2 = nextConfig.r2;
		this.r2Prefix = nextConfig.r2Prefix;
		this.threshold = nextConfig.inlineThreshold;
		this.onChange = options.onChange;
	}
	get _name() {
		const v = this._nameOrFn;
		return typeof v === "function" ? v() : v;
	}
	emit(type, path, entryType) {
		if (this.onChange) this.onChange({
			type,
			path,
			entryType
		});
	}
	_observe(type, payload) {
		wsChannel.publish({
			type,
			name: this._name,
			payload: {
				...payload,
				namespace: this.namespace
			},
			timestamp: Date.now()
		});
	}
	async ensureInit() {
		if (this.initialized) return;
		this.initialized = true;
		const T = this.tableName;
		const I = this.indexName;
		await this.sql.run(`
      CREATE TABLE IF NOT EXISTS ${T} (
        path            TEXT PRIMARY KEY,
        parent_path     TEXT NOT NULL,
        name            TEXT NOT NULL,
        type            TEXT NOT NULL CHECK(type IN ('file','directory','symlink')),
        mime_type       TEXT NOT NULL DEFAULT 'text/plain',
        size            INTEGER NOT NULL DEFAULT 0,
        storage_backend TEXT NOT NULL DEFAULT 'inline' CHECK(storage_backend IN ('inline','r2')),
        r2_key          TEXT,
        target          TEXT,
        content_encoding TEXT NOT NULL DEFAULT 'utf8',
        content         TEXT,
        created_at      INTEGER NOT NULL DEFAULT (unixepoch()),
        modified_at     INTEGER NOT NULL DEFAULT (unixepoch())
      )
    `);
		await this.sql.run(`CREATE INDEX IF NOT EXISTS ${I} ON ${T}(parent_path)`);
		if (((await this.sql.query(`SELECT COUNT(*) AS cnt FROM ${T} WHERE path = '/'`))[0]?.cnt ?? 0) === 0) {
			const now = Math.floor(Date.now() / 1e3);
			await this.sql.run(`INSERT INTO ${T}
          (path, parent_path, name, type, size, created_at, modified_at)
        VALUES ('/', '', '', 'directory', 0, ?, ?)`, now, now);
		}
	}
	getR2() {
		return this.r2;
	}
	resolveR2Prefix() {
		if (this.r2Prefix !== void 0) return this.r2Prefix;
		const name = this._name;
		if (!name) throw new Error("[Workspace] R2 is configured but no r2Prefix was provided and no name is available. Either pass r2Prefix in WorkspaceOptions or provide a name.");
		return name;
	}
	r2Key(filePath) {
		return `${this.resolveR2Prefix()}/${this.namespace}${filePath}`;
	}
	async resolveSymlink(path, depth = 0) {
		if (depth > MAX_SYMLINK_DEPTH) throw new Error(`ELOOP: too many levels of symbolic links: ${path}`);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT type, target FROM ${T} WHERE path = ?`, path))[0];
		if (!r || r.type !== "symlink" || !r.target) return path;
		const resolved = r.target.startsWith("/") ? normalizePath(r.target) : normalizePath(getParent(path) + "/" + r.target);
		return this.resolveSymlink(resolved, depth + 1);
	}
	async symlink(target, linkPath) {
		await this.ensureInit();
		if (!target || target.trim().length === 0) throw new Error("EINVAL: symlink target must not be empty");
		if (target.length > MAX_SYMLINK_TARGET_LENGTH) throw new Error(`ENAMETOOLONG: symlink target exceeds ${MAX_SYMLINK_TARGET_LENGTH} characters`);
		const normalized = normalizePath(linkPath);
		if (normalized === "/") throw new Error("EPERM: cannot create symlink at root");
		const parentPath = getParent(normalized);
		const name = getBasename(normalized);
		const now = Math.floor(Date.now() / 1e3);
		const T = this.tableName;
		await this.ensureParentDir(parentPath);
		if ((await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, normalized))[0]) throw new Error(`EEXIST: path already exists: ${linkPath}`);
		await this.sql.run(`INSERT INTO ${T}
        (path, parent_path, name, type, target, size, created_at, modified_at)
      VALUES (?, ?, ?, 'symlink', ?, 0, ?, ?)`, normalized, parentPath, name, target, now, now);
		this.emit("create", normalized, "symlink");
	}
	async readlink(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT type, target FROM ${T} WHERE path = ?`, normalized))[0];
		if (!r) throw new Error(`ENOENT: no such file or directory: ${path}`);
		if (r.type !== "symlink" || !r.target) throw new Error(`EINVAL: not a symlink: ${path}`);
		return r.target;
	}
	async lstat(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT path, name, type, mime_type, size, created_at, modified_at, target
      FROM ${T} WHERE path = ?`, normalized))[0];
		if (!r) return null;
		return toFileInfo(r);
	}
	async stat(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const resolved = await this.resolveSymlink(normalized);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT path, name, type, mime_type, size, created_at, modified_at, target
      FROM ${T} WHERE path = ?`, resolved))[0];
		if (!r) return null;
		return toFileInfo(r);
	}
	async readFile(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const resolved = await this.resolveSymlink(normalized);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT type, storage_backend, r2_key, content, content_encoding
      FROM ${T} WHERE path = ?`, resolved))[0];
		if (!r) return null;
		if (r.type !== "file") throw new Error(`EISDIR: ${path} is a directory`);
		this._observe("workspace:read", {
			path: resolved,
			storage: r.storage_backend
		});
		if (r.storage_backend === "r2" && r.r2_key) {
			const r2 = this.getR2();
			if (!r2) throw new Error(`File ${path} is stored in R2 but no R2 bucket was provided`);
			const obj = await r2.get(r.r2_key);
			if (!obj) return "";
			return await obj.text();
		}
		if (r.content_encoding === "base64" && r.content) {
			const bytes = base64ToBytes(r.content);
			return TEXT_DECODER.decode(bytes);
		}
		return r.content ?? "";
	}
	async readFileBytes(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const resolved = await this.resolveSymlink(normalized);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT type, storage_backend, r2_key, content, content_encoding
      FROM ${T} WHERE path = ?`, resolved))[0];
		if (!r) return null;
		if (r.type !== "file") throw new Error(`EISDIR: ${path} is a directory`);
		this._observe("workspace:read", {
			path: resolved,
			storage: r.storage_backend
		});
		if (r.storage_backend === "r2" && r.r2_key) {
			const r2 = this.getR2();
			if (!r2) throw new Error(`File ${path} is stored in R2 but no R2 bucket was provided`);
			const obj = await r2.get(r.r2_key);
			if (!obj) return new Uint8Array(0);
			return new Uint8Array(await obj.arrayBuffer());
		}
		if (r.content_encoding === "base64" && r.content) return base64ToBytes(r.content);
		return TEXT_ENCODER.encode(r.content ?? "");
	}
	async writeFileBytes(path, data, mimeType = "application/octet-stream") {
		await this.ensureInit();
		const normalized = await this.resolveSymlink(normalizePath(path));
		if (normalized === "/") throw new Error("EISDIR: cannot write to root directory");
		const bytes = normalizeBytes(data);
		const size = bytes.byteLength;
		const parentPath = getParent(normalized);
		const name = getBasename(normalized);
		const now = Math.floor(Date.now() / 1e3);
		const T = this.tableName;
		await this.ensureParentDir(parentPath);
		const existing = (await this.sql.query(`SELECT storage_backend, r2_key FROM ${T} WHERE path = ?`, normalized))[0];
		const r2 = this.getR2();
		if (size >= this.threshold && r2) {
			const key = this.r2Key(normalized);
			if (existing?.storage_backend === "r2" && existing.r2_key !== key) await r2.delete(existing.r2_key);
			await r2.put(key, bytes, { httpMetadata: { contentType: mimeType } });
			try {
				await this.sql.run(`INSERT INTO ${T}
            (path, parent_path, name, type, mime_type, size,
             storage_backend, r2_key, content_encoding, content, created_at, modified_at)
          VALUES (?, ?, ?, 'file', ?, ?, 'r2', ?, 'base64', NULL, ?, ?)
          ON CONFLICT(path) DO UPDATE SET
            mime_type         = excluded.mime_type,
            size              = excluded.size,
            storage_backend   = 'r2',
            r2_key            = excluded.r2_key,
            content_encoding  = 'base64',
            content           = NULL,
            modified_at       = excluded.modified_at`, normalized, parentPath, name, mimeType, size, key, now, now);
			} catch (sqlErr) {
				try {
					await r2.delete(key);
				} catch {
					console.error(`[Workspace] Failed to clean up orphaned R2 object ${key} after SQL error`);
				}
				throw sqlErr;
			}
			this.emit(existing ? "update" : "create", normalized, "file");
			this._observe("workspace:write", {
				path: normalized,
				size,
				storage: "r2",
				update: !!existing
			});
		} else {
			if (size >= this.threshold && !r2) console.warn(`[Workspace] File ${path} is ${size} bytes but no R2 bucket was provided. Storing inline.`);
			if (existing?.storage_backend === "r2" && existing.r2_key && r2) await r2.delete(existing.r2_key);
			const b64 = bytesToBase64(bytes);
			await this.sql.run(`INSERT INTO ${T}
          (path, parent_path, name, type, mime_type, size,
           storage_backend, r2_key, content_encoding, content, created_at, modified_at)
        VALUES (?, ?, ?, 'file', ?, ?, 'inline', NULL, 'base64', ?, ?, ?)
        ON CONFLICT(path) DO UPDATE SET
          mime_type         = excluded.mime_type,
          size              = excluded.size,
          storage_backend   = 'inline',
          r2_key            = NULL,
          content_encoding  = 'base64',
          content           = excluded.content,
          modified_at       = excluded.modified_at`, normalized, parentPath, name, mimeType, size, b64, now, now);
			this.emit(existing ? "update" : "create", normalized, "file");
			this._observe("workspace:write", {
				path: normalized,
				size,
				storage: "inline",
				update: !!existing
			});
		}
	}
	async writeFile(path, content, mimeType = "text/plain") {
		await this.ensureInit();
		const normalized = await this.resolveSymlink(normalizePath(path));
		if (normalized === "/") throw new Error("EISDIR: cannot write to root directory");
		const parentPath = getParent(normalized);
		const name = getBasename(normalized);
		const bytes = TEXT_ENCODER.encode(content);
		const size = bytes.byteLength;
		const now = Math.floor(Date.now() / 1e3);
		const T = this.tableName;
		await this.ensureParentDir(parentPath);
		const existing = (await this.sql.query(`SELECT storage_backend, r2_key FROM ${T} WHERE path = ?`, normalized))[0];
		const r2 = this.getR2();
		if (size >= this.threshold && r2) {
			const key = this.r2Key(normalized);
			if (existing?.storage_backend === "r2" && existing.r2_key !== key) await r2.delete(existing.r2_key);
			await r2.put(key, bytes, { httpMetadata: { contentType: mimeType } });
			try {
				await this.sql.run(`INSERT INTO ${T}
            (path, parent_path, name, type, mime_type, size,
             storage_backend, r2_key, content_encoding, content, created_at, modified_at)
          VALUES (?, ?, ?, 'file', ?, ?, 'r2', ?, 'utf8', NULL, ?, ?)
          ON CONFLICT(path) DO UPDATE SET
            mime_type         = excluded.mime_type,
            size              = excluded.size,
            storage_backend   = 'r2',
            r2_key            = excluded.r2_key,
            content_encoding  = 'utf8',
            content           = NULL,
            modified_at       = excluded.modified_at`, normalized, parentPath, name, mimeType, size, key, now, now);
			} catch (sqlErr) {
				try {
					await r2.delete(key);
				} catch {
					console.error(`[Workspace] Failed to clean up orphaned R2 object ${key} after SQL error`);
				}
				throw sqlErr;
			}
			this.emit(existing ? "update" : "create", normalized, "file");
			this._observe("workspace:write", {
				path: normalized,
				size,
				storage: "r2",
				update: !!existing
			});
		} else {
			if (size >= this.threshold && !r2) console.warn(`[Workspace] File ${path} is ${size} bytes but no R2 bucket was provided. Storing inline — this may hit SQLite row limits for very large files.`);
			if (existing?.storage_backend === "r2" && existing.r2_key && r2) await r2.delete(existing.r2_key);
			await this.sql.run(`INSERT INTO ${T}
          (path, parent_path, name, type, mime_type, size,
           storage_backend, r2_key, content_encoding, content, created_at, modified_at)
        VALUES (?, ?, ?, 'file', ?, ?, 'inline', NULL, 'utf8', ?, ?, ?)
        ON CONFLICT(path) DO UPDATE SET
          mime_type         = excluded.mime_type,
          size              = excluded.size,
          storage_backend   = 'inline',
          r2_key            = NULL,
          content_encoding  = 'utf8',
          content           = excluded.content,
          modified_at       = excluded.modified_at`, normalized, parentPath, name, mimeType, size, content, now, now);
			this.emit(existing ? "update" : "create", normalized, "file");
			this._observe("workspace:write", {
				path: normalized,
				size,
				storage: "inline",
				update: !!existing
			});
		}
	}
	async readFileStream(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const resolved = await this.resolveSymlink(normalized);
		const T = this.tableName;
		const r = (await this.sql.query(`SELECT type, storage_backend, r2_key, content, content_encoding
      FROM ${T} WHERE path = ?`, resolved))[0];
		if (!r) return null;
		if (r.type !== "file") throw new Error(`EISDIR: ${path} is a directory`);
		this._observe("workspace:read", {
			path: resolved,
			storage: r.storage_backend
		});
		if (r.storage_backend === "r2" && r.r2_key) {
			const r2 = this.getR2();
			if (!r2) throw new Error(`File ${path} is stored in R2 but no R2 bucket was provided`);
			const obj = await r2.get(r.r2_key);
			if (!obj) return new ReadableStream({ start(c) {
				c.close();
			} });
			return obj.body;
		}
		const bytes = r.content_encoding === "base64" && r.content ? base64ToBytes(r.content) : TEXT_ENCODER.encode(r.content ?? "");
		return new ReadableStream({ start(controller) {
			controller.enqueue(bytes);
			controller.close();
		} });
	}
	async writeFileStream(path, stream, mimeType = "application/octet-stream") {
		const reader = stream.getReader();
		const chunks = [];
		let totalSize = 0;
		for (;;) {
			const { done, value } = await reader.read();
			if (done) break;
			totalSize += value.byteLength;
			if (totalSize > MAX_STREAM_SIZE) {
				reader.cancel();
				throw new Error(`EFBIG: stream exceeds maximum size of ${MAX_STREAM_SIZE} bytes`);
			}
			chunks.push(value);
		}
		const buffer = new Uint8Array(totalSize);
		let offset = 0;
		for (const chunk of chunks) {
			buffer.set(chunk, offset);
			offset += chunk.byteLength;
		}
		await this.writeFileBytes(path, buffer, mimeType);
	}
	async appendFile(path, content, mimeType = "text/plain") {
		await this.ensureInit();
		const normalized = await this.resolveSymlink(normalizePath(path));
		const T = this.tableName;
		const row = (await this.sql.query(`SELECT type, storage_backend, content_encoding
        FROM ${T} WHERE path = ?`, normalized))[0];
		if (!row) {
			await this.writeFile(path, content, mimeType);
			return;
		}
		if (row.type !== "file") throw new Error(`EISDIR: ${path} is a directory`);
		if (row.storage_backend === "inline" && row.content_encoding === "utf8") {
			const appendSize = TEXT_ENCODER.encode(content).byteLength;
			const now = Math.floor(Date.now() / 1e3);
			await this.sql.run(`UPDATE ${T} SET
          content = content || ?,
          size = size + ?,
          modified_at = ?
        WHERE path = ?`, content, appendSize, now, normalized);
			this.emit("update", normalized, "file");
			return;
		}
		const existing = await this.readFile(path);
		await this.writeFile(path, (existing ?? "") + content, mimeType);
	}
	async deleteFile(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const T = this.tableName;
		const rows = await this.sql.query(`SELECT type, storage_backend, r2_key FROM ${T} WHERE path = ?`, normalized);
		if (!rows[0]) return false;
		if (rows[0].type === "directory") throw new Error(`EISDIR: ${path} is a directory — use rm() instead`);
		if (rows[0].storage_backend === "r2" && rows[0].r2_key) {
			const r2 = this.getR2();
			if (r2) await r2.delete(rows[0].r2_key);
		}
		await this.sql.run(`DELETE FROM ${T} WHERE path = ?`, normalized);
		this.emit("delete", normalized, rows[0].type);
		this._observe("workspace:delete", { path: normalized });
		return true;
	}
	async fileExists(path) {
		await this.ensureInit();
		const resolved = await this.resolveSymlink(normalizePath(path));
		const T = this.tableName;
		const rows = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, resolved);
		return rows.length > 0 && rows[0].type === "file";
	}
	async exists(path) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const T = this.tableName;
		return ((await this.sql.query(`SELECT COUNT(*) AS cnt FROM ${T} WHERE path = ?`, normalized))[0]?.cnt ?? 0) > 0;
	}
	async readDir(dir = "/", opts) {
		await this.ensureInit();
		const normalized = normalizePath(dir);
		const limit = opts?.limit ?? 1e3;
		const offset = opts?.offset ?? 0;
		const T = this.tableName;
		return (await this.sql.query(`SELECT path, name, type, mime_type, size, created_at, modified_at
      FROM ${T}
      WHERE parent_path = ?
      ORDER BY type ASC, name ASC
      LIMIT ? OFFSET ?`, normalized, limit, offset)).map(toFileInfo);
	}
	async glob(pattern) {
		await this.ensureInit();
		const normalized = normalizePath(pattern);
		const likePattern = escapeLike(getGlobPrefix(normalized)) + "%";
		const regex = globToRegex(normalized);
		const T = this.tableName;
		return (await this.sql.query(`SELECT path, name, type, mime_type, size, created_at, modified_at, target
      FROM ${T}
      WHERE path LIKE ? ESCAPE ?
      ORDER BY path`, likePattern, LIKE_ESCAPE)).filter((r) => regex.test(r.path)).map(toFileInfo);
	}
	async mkdir(path, opts, _depth = 0) {
		await this.ensureInit();
		if (_depth > MAX_MKDIR_DEPTH) throw new Error(`ELOOP: mkdir recursion too deep (max ${MAX_MKDIR_DEPTH} levels)`);
		const normalized = normalizePath(path);
		if (normalized === "/") return;
		const T = this.tableName;
		const existing = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, normalized);
		if (existing.length > 0) {
			if (existing[0].type === "directory" && opts?.recursive) return;
			throw new Error(existing[0].type === "directory" ? `EEXIST: directory already exists: ${path}` : `EEXIST: path exists as a file: ${path}`);
		}
		const parentPath = getParent(normalized);
		const parentRows = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, parentPath);
		if (!parentRows[0]) if (opts?.recursive) await this.mkdir(parentPath, { recursive: true }, _depth + 1);
		else throw new Error(`ENOENT: parent directory not found: ${parentPath}`);
		else if (parentRows[0].type !== "directory") throw new Error(`ENOTDIR: parent is not a directory: ${parentPath}`);
		const name = getBasename(normalized);
		const now = Math.floor(Date.now() / 1e3);
		await this.sql.run(`INSERT INTO ${T}
        (path, parent_path, name, type, size, created_at, modified_at)
      VALUES (?, ?, ?, 'directory', 0, ?, ?)`, normalized, parentPath, name, now, now);
		this.emit("create", normalized, "directory");
		this._observe("workspace:mkdir", {
			path: normalized,
			recursive: !!opts?.recursive
		});
	}
	async rm(path, opts) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		if (normalized === "/") throw new Error("EPERM: cannot remove root directory");
		const T = this.tableName;
		const rows = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, normalized);
		if (!rows[0]) {
			if (opts?.force) return;
			throw new Error(`ENOENT: no such file or directory: ${path}`);
		}
		if (rows[0].type === "directory") {
			if (((await this.sql.query(`SELECT COUNT(*) AS cnt FROM ${T} WHERE parent_path = ?`, normalized))[0]?.cnt ?? 0) > 0) {
				if (!opts?.recursive) throw new Error(`ENOTEMPTY: directory not empty: ${path}`);
				await this.deleteDescendants(normalized);
			}
		} else {
			const fileRow = (await this.sql.query(`SELECT storage_backend, r2_key FROM ${T} WHERE path = ?`, normalized))[0];
			if (fileRow?.storage_backend === "r2" && fileRow.r2_key) {
				const r2 = this.getR2();
				if (r2) await r2.delete(fileRow.r2_key);
			}
		}
		await this.sql.run(`DELETE FROM ${T} WHERE path = ?`, normalized);
		this.emit("delete", normalized, rows[0].type);
		this._observe("workspace:rm", {
			path: normalized,
			recursive: !!opts?.recursive
		});
	}
	async cp(src, dest, opts) {
		await this.ensureInit();
		const srcNorm = normalizePath(src);
		const destNorm = normalizePath(dest);
		const srcStat = await this.lstat(srcNorm);
		if (!srcStat) throw new Error(`ENOENT: no such file or directory: ${src}`);
		if (srcStat.type === "symlink") {
			const target = await this.readlink(srcNorm);
			await this.symlink(target, destNorm);
			return;
		}
		if (srcStat.type === "directory") {
			if (!opts?.recursive) throw new Error(`EISDIR: cannot copy directory without recursive: ${src}`);
			await this.mkdir(destNorm, { recursive: true });
			for (const child of await this.readDir(srcNorm)) await this.cp(child.path, `${destNorm}/${child.name}`, opts);
			return;
		}
		const bytes = await this.readFileBytes(srcNorm);
		if (bytes) await this.writeFileBytes(destNorm, bytes, srcStat.mimeType);
		else await this.writeFile(destNorm, "", srcStat.mimeType);
		this._observe("workspace:cp", {
			src: srcNorm,
			dest: destNorm,
			recursive: !!opts?.recursive
		});
	}
	async mv(src, dest, opts) {
		await this.ensureInit();
		const srcNorm = normalizePath(src);
		const destNorm = normalizePath(dest);
		const srcStat = await this.lstat(srcNorm);
		if (!srcStat) throw new Error(`ENOENT: no such file or directory: ${src}`);
		if (srcStat.type === "directory") {
			if (!(opts?.recursive ?? true)) throw new Error(`EISDIR: cannot move directory without recursive: ${src}`);
			await this.cp(src, dest, { recursive: true });
			await this.rm(src, {
				recursive: true,
				force: true
			});
			return;
		}
		const destParent = getParent(destNorm);
		const destName = getBasename(destNorm);
		const T = this.tableName;
		await this.ensureParentDir(destParent);
		const existingDest = (await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, destNorm))[0];
		if (existingDest) {
			if (existingDest.type === "directory") throw new Error(`EISDIR: cannot overwrite directory: ${dest}`);
			await this.deleteFile(destNorm);
		}
		if (srcStat.type === "file") {
			const row = (await this.sql.query(`SELECT storage_backend, r2_key FROM ${T} WHERE path = ?`, srcNorm))[0];
			if (row?.storage_backend === "r2" && row.r2_key) {
				const r2 = this.getR2();
				if (r2) {
					const newKey = this.r2Key(destNorm);
					const obj = await r2.get(row.r2_key);
					if (obj) await r2.put(newKey, await obj.arrayBuffer(), { httpMetadata: obj.httpMetadata });
					await r2.delete(row.r2_key);
					const now = Math.floor(Date.now() / 1e3);
					await this.sql.run(`UPDATE ${T} SET
              path = ?,
              parent_path = ?,
              name = ?,
              r2_key = ?,
              modified_at = ?
            WHERE path = ?`, destNorm, destParent, destName, newKey, now, srcNorm);
					this.emit("delete", srcNorm, "file");
					this.emit("create", destNorm, "file");
					this._observe("workspace:mv", {
						src: srcNorm,
						dest: destNorm
					});
					return;
				}
			}
		}
		const now = Math.floor(Date.now() / 1e3);
		await this.sql.run(`UPDATE ${T} SET
        path = ?,
        parent_path = ?,
        name = ?,
        modified_at = ?
      WHERE path = ?`, destNorm, destParent, destName, now, srcNorm);
		this.emit("delete", srcNorm, srcStat.type);
		this.emit("create", destNorm, srcStat.type);
		this._observe("workspace:mv", {
			src: srcNorm,
			dest: destNorm
		});
	}
	async diff(pathA, pathB) {
		const contentA = await this.readFile(pathA);
		if (contentA === null) throw new Error(`ENOENT: no such file: ${pathA}`);
		const contentB = await this.readFile(pathB);
		if (contentB === null) throw new Error(`ENOENT: no such file: ${pathB}`);
		const linesA = contentA.split("\n").length;
		const linesB = contentB.split("\n").length;
		if (linesA > MAX_DIFF_LINES || linesB > MAX_DIFF_LINES) throw new Error(`EFBIG: files too large for diff (max ${MAX_DIFF_LINES} lines)`);
		return unifiedDiff(contentA, contentB, normalizePath(pathA), normalizePath(pathB));
	}
	async diffContent(path, newContent) {
		const existing = await this.readFile(path);
		if (existing === null) throw new Error(`ENOENT: no such file: ${path}`);
		const linesA = existing.split("\n").length;
		const linesB = newContent.split("\n").length;
		if (linesA > MAX_DIFF_LINES || linesB > MAX_DIFF_LINES) throw new Error(`EFBIG: content too large for diff (max ${MAX_DIFF_LINES} lines)`);
		const normalized = normalizePath(path);
		return unifiedDiff(existing, newContent, normalized, normalized);
	}
	async getWorkspaceInfo() {
		await this.ensureInit();
		const T = this.tableName;
		const rows = await this.sql.query(`SELECT
        SUM(CASE WHEN type = 'file'                               THEN 1 ELSE 0 END) AS files,
        SUM(CASE WHEN type = 'directory'                          THEN 1 ELSE 0 END) AS dirs,
        COALESCE(SUM(CASE WHEN type = 'file' THEN size ELSE 0 END), 0)               AS total,
        SUM(CASE WHEN type = 'file' AND storage_backend = 'r2'   THEN 1 ELSE 0 END) AS r2files
      FROM ${T}`);
		return {
			fileCount: rows[0]?.files ?? 0,
			directoryCount: rows[0]?.dirs ?? 0,
			totalBytes: rows[0]?.total ?? 0,
			r2FileCount: rows[0]?.r2files ?? 0
		};
	}
	/** @internal */
	async _getAllPaths() {
		await this.ensureInit();
		const T = this.tableName;
		return (await this.sql.query(`SELECT path FROM ${T} ORDER BY path`)).map((r) => r.path);
	}
	/** @internal */
	async _updateModifiedAt(path, mtime) {
		await this.ensureInit();
		const normalized = normalizePath(path);
		const ts = Math.floor(mtime.getTime() / 1e3);
		const T = this.tableName;
		await this.sql.run(`UPDATE ${T} SET modified_at = ? WHERE path = ?`, ts, normalized);
	}
	async ensureParentDir(dirPath) {
		if (!dirPath || dirPath === "/") return;
		const T = this.tableName;
		const rows = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, dirPath);
		if (rows[0]) {
			if (rows[0].type !== "directory") throw new Error(`ENOTDIR: ${dirPath} is not a directory`);
			return;
		}
		const missing = [dirPath];
		let current = getParent(dirPath);
		while (current && current !== "/") {
			const r = await this.sql.query(`SELECT type FROM ${T} WHERE path = ?`, current);
			if (r[0]) {
				if (r[0].type !== "directory") throw new Error(`ENOTDIR: ${current} is not a directory`);
				break;
			}
			missing.push(current);
			current = getParent(current);
		}
		const now = Math.floor(Date.now() / 1e3);
		for (let i = missing.length - 1; i >= 0; i--) {
			const p = missing[i];
			const parentPath = getParent(p);
			const name = getBasename(p);
			await this.sql.run(`INSERT INTO ${T}
          (path, parent_path, name, type, size, created_at, modified_at)
        VALUES (?, ?, ?, 'directory', 0, ?, ?)`, p, parentPath, name, now, now);
			this.emit("create", p, "directory");
		}
	}
	async deleteDescendants(dirPath) {
		const pattern = escapeLike(dirPath) + "/%";
		const T = this.tableName;
		const r2Rows = await this.sql.query(`SELECT r2_key FROM ${T}
      WHERE path LIKE ? ESCAPE ?
        AND storage_backend = 'r2'
        AND r2_key IS NOT NULL`, pattern, LIKE_ESCAPE);
		if (r2Rows.length > 0) {
			const r2 = this.getR2();
			if (r2) {
				const keys = r2Rows.map((r) => r.r2_key);
				await r2.delete(keys);
			}
		}
		await this.sql.run(`DELETE FROM ${T} WHERE path LIKE ? ESCAPE ?`, pattern, LIKE_ESCAPE);
	}
};
function normalizeBytes(data) {
	if (data instanceof Uint8Array) return data;
	if (data instanceof ArrayBuffer) return new Uint8Array(data);
	throw new TypeError("writeFileBytes expected Uint8Array or ArrayBuffer");
}
function bytesToBase64(bytes) {
	const CHUNK = 8192;
	let binary = "";
	for (let i = 0; i < bytes.byteLength; i += CHUNK) binary += String.fromCharCode(...bytes.subarray(i, Math.min(i + CHUNK, bytes.byteLength)));
	return btoa(binary);
}
function base64ToBytes(b64) {
	const binary = atob(b64);
	const bytes = new Uint8Array(binary.length);
	for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
	return bytes;
}
function escapeLike(s) {
	return s.replace(/[\\%_]/g, (ch) => "\\" + ch);
}
function normalizePath(path) {
	if (!path.startsWith("/")) path = "/" + path;
	const parts = path.split("/");
	const resolved = [];
	for (const part of parts) {
		if (part === "" || part === ".") continue;
		if (part === "..") resolved.pop();
		else resolved.push(part);
	}
	const result = "/" + resolved.join("/");
	if (result.length > MAX_PATH_LENGTH) throw new Error(`ENAMETOOLONG: path exceeds ${MAX_PATH_LENGTH} characters`);
	return result;
}
function getParent(path) {
	const normalized = normalizePath(path);
	if (normalized === "/") return "";
	const lastSlash = normalized.lastIndexOf("/");
	return lastSlash === 0 ? "/" : normalized.slice(0, lastSlash);
}
function getBasename(path) {
	const normalized = normalizePath(path);
	if (normalized === "/") return "";
	return normalized.slice(normalized.lastIndexOf("/") + 1);
}
function toFileInfo(r) {
	const info = {
		path: r.path,
		name: r.name,
		type: r.type,
		mimeType: r.mime_type,
		size: r.size,
		createdAt: r.created_at * 1e3,
		updatedAt: r.modified_at * 1e3
	};
	if (r.target) info.target = r.target;
	return info;
}
function getGlobPrefix(pattern) {
	const first = pattern.search(/[*?[{]/);
	if (first === -1) return pattern;
	const before = pattern.slice(0, first);
	const lastSlash = before.lastIndexOf("/");
	return lastSlash >= 0 ? before.slice(0, lastSlash + 1) : "/";
}
function globToRegex(pattern) {
	let i = 0;
	let re = "^";
	while (i < pattern.length) {
		const ch = pattern[i];
		if (ch === "*") if (pattern[i + 1] === "*") {
			i += 2;
			if (pattern[i] === "/") {
				re += "(?:.+/)?";
				i++;
			} else re += ".*";
		} else {
			re += "[^/]*";
			i++;
		}
		else if (ch === "?") {
			re += "[^/]";
			i++;
		} else if (ch === "[") {
			const close = pattern.indexOf("]", i + 1);
			if (close === -1) {
				re += "\\[";
				i++;
			} else {
				re += pattern.slice(i, close + 1);
				i = close + 1;
			}
		} else if (ch === "{") {
			const close = pattern.indexOf("}", i + 1);
			if (close === -1) {
				re += "\\{";
				i++;
			} else {
				const inner = pattern.slice(i + 1, close).split(",").join("|");
				re += `(?:${inner})`;
				i = close + 1;
			}
		} else {
			re += ch.replace(/[.+^$|\\()]/g, "\\$&");
			i++;
		}
	}
	re += "$";
	return new RegExp(re);
}
function unifiedDiff(a, b, labelA, labelB, contextLines = 3) {
	if (a === b) return "";
	const linesA = a.split("\n");
	const linesB = b.split("\n");
	return formatUnified(myersDiff(linesA, linesB), linesA, linesB, labelA, labelB, contextLines);
}
function myersDiff(a, b) {
	const n = a.length;
	const m = b.length;
	const max = n + m;
	const vSize = 2 * max + 1;
	const v = new Int32Array(vSize);
	v.fill(-1);
	const offset = max;
	v[offset + 1] = 0;
	const trace = [];
	outer: for (let d = 0; d <= max; d++) {
		trace.push(v.slice());
		for (let k = -d; k <= d; k += 2) {
			let x;
			if (k === -d || k !== d && v[offset + k - 1] < v[offset + k + 1]) x = v[offset + k + 1];
			else x = v[offset + k - 1] + 1;
			let y = x - k;
			while (x < n && y < m && a[x] === b[y]) {
				x++;
				y++;
			}
			v[offset + k] = x;
			if (x >= n && y >= m) break outer;
		}
	}
	const edits = [];
	let x = n;
	let y = m;
	for (let d = trace.length - 1; d >= 0; d--) {
		const vPrev = trace[d];
		const k = x - y;
		let prevK;
		if (k === -d || k !== d && vPrev[offset + k - 1] < vPrev[offset + k + 1]) prevK = k + 1;
		else prevK = k - 1;
		const prevX = vPrev[offset + prevK];
		const prevY = prevX - prevK;
		while (x > prevX && y > prevY) {
			x--;
			y--;
			edits.push({
				type: "keep",
				lineA: x,
				lineB: y
			});
		}
		if (d > 0) if (x === prevX) {
			edits.push({
				type: "insert",
				lineA: x,
				lineB: y - 1
			});
			y--;
		} else {
			edits.push({
				type: "delete",
				lineA: x - 1,
				lineB: y
			});
			x--;
		}
	}
	edits.reverse();
	return edits;
}
function formatUnified(edits, linesA, linesB, labelA, labelB, ctx) {
	const out = [];
	out.push(`--- ${labelA}`);
	out.push(`+++ ${labelB}`);
	const changes = [];
	for (let i = 0; i < edits.length; i++) if (edits[i].type !== "keep") changes.push(i);
	if (changes.length === 0) return "";
	let i = 0;
	while (i < changes.length) {
		let start = Math.max(0, changes[i] - ctx);
		let end = Math.min(edits.length - 1, changes[i] + ctx);
		let j = i + 1;
		while (j < changes.length && changes[j] - ctx <= end + 1) {
			end = Math.min(edits.length - 1, changes[j] + ctx);
			j++;
		}
		let startA = edits[start].lineA;
		let startB = edits[start].lineB;
		let countA = 0;
		let countB = 0;
		const hunkLines = [];
		for (let idx = start; idx <= end; idx++) {
			const e = edits[idx];
			if (e.type === "keep") {
				hunkLines.push(` ${linesA[e.lineA]}`);
				countA++;
				countB++;
			} else if (e.type === "delete") {
				hunkLines.push(`-${linesA[e.lineA]}`);
				countA++;
			} else {
				hunkLines.push(`+${linesB[e.lineB]}`);
				countB++;
			}
		}
		out.push(`@@ -${startA + 1},${countA} +${startB + 1},${countB} @@`);
		out.push(...hunkLines);
		i = j;
	}
	return out.join("\n");
}
//#endregion
export { FileSystemStateBackend, InMemoryFs, STATE_SYSTEM_PROMPT, STATE_TYPES, StateBatchOperationError, Workspace, WorkspaceFileSystem, createMemoryStateBackend, createWorkspaceStateBackend };

//# sourceMappingURL=index.js.map