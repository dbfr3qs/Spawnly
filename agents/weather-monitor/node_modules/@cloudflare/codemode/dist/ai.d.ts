import {
  c as ResolvedProvider,
  d as generateTypes,
  i as ToolProvider,
  l as ToolDescriptor,
  u as ToolDescriptors
} from "./executor-BRm90XJT.js";
import {
  n as CodeOutput,
  r as CreateCodeToolOptions,
  t as CodeInput
} from "./shared-TyFN6W7_.js";
import { Tool, ToolSet } from "ai";

//#region src/tool.d.ts
/**
 * Wrap raw AI SDK tools into a ToolProvider under the default "codemode" namespace.
 *
 * @example
 * ```ts
 * createCodeTool({
 *   tools: [stateTools(workspace), aiTools(myTools)],
 *   executor,
 * });
 * ```
 */
declare function aiTools(tools: ToolDescriptors | ToolSet): ToolProvider;
/**
 * Resolve a ToolProvider into a ResolvedProvider ready for execution.
 * Filters out tools with `needsApproval` and validates inputs via AI SDK's `asSchema`.
 */
declare function resolveProvider(provider: ToolProvider): ResolvedProvider;
declare function createCodeTool(
  options: CreateCodeToolOptions
): Tool<CodeInput, CodeOutput>;
//#endregion
export {
  type CodeInput,
  type CodeOutput,
  type CreateCodeToolOptions,
  type ToolDescriptor,
  type ToolDescriptors,
  aiTools,
  createCodeTool,
  generateTypes,
  resolveProvider
};
//# sourceMappingURL=ai.d.ts.map
