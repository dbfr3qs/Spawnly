import { C as SessionData, E as SessionStore, c as FlueEvent, m as ModelConfig } from "./types-CylqjtIR.mjs";
import { C as RunSubscriberRegistry, D as createFlueContext, E as FlueContextInternal, S as RunSubscriberListener, T as FlueContextConfig, _ as CreateContextFn, b as StartWebhookFn, d as configureFlueRuntime, f as createDefaultFlueApp, g as AgentHandler, h as handleRunRouteRequest, m as HandleRunRouteOptions, s as hasRegisteredProvider, u as FlueRuntime, v as HandleAgentOptions, w as createRunSubscriberRegistry, x as handleAgentRequest, y as RunHandlerFn } from "./providers-B_Gun4UP.mjs";
import { _ as RunStore, a as ListInstancesOpts, c as ListRunsResponse, d as RunPointer, f as RunRegistry, g as RunStatus, h as RunRecord, i as InstancePointer, l as RecordRunEndInput, m as EndRunInput, o as ListInstancesResponse, p as CreateRunInput, s as ListRunsOpts, u as RecordRunStartInput, v as RunStoreOptions } from "./cloudflare-model-BDxkNr2S.mjs";
import { n as bashFactoryToSessionEnv } from "./sandbox-UtFfPX4w.mjs";
import { Api, Model } from "@earendil-works/pi-ai";
import { AgentMessage } from "@earendil-works/pi-agent-core";

//#region src/context.d.ts
interface FrontmatterResult {
  name: string;
  description: string;
  body: string;
  frontmatter: Record<string, string>;
}
/** Parse optional YAML frontmatter (--- delimited). Basic `key: value` only. */
declare function parseFrontmatterFile(content: string, defaultName: string): FrontmatterResult;
//#endregion
//#region src/cloudflare/run-store.d.ts
interface SqlResult {
  toArray(): SqlRow[];
}
type SqlRow = Record<string, unknown>;
interface SqlStorage {
  exec(query: string, ...bindings: unknown[]): SqlResult;
}
declare function createDurableRunStore(sql: SqlStorage, options?: RunStoreOptions): RunStore;
//#endregion
//#region src/node/run-registry.d.ts
interface InMemoryRunRegistryOptions {
  /**
   * Per-instance cap on retained completed pointers. Defaults to 50,
   * mirroring `InMemoryRunStore`'s per-instance run cap. Active runs
   * are never pruned.
   */
  maxCompletedRunsPerInstance?: number;
}
declare class InMemoryRunRegistry implements RunRegistry {
  private pointers;
  private byInstance;
  private instances;
  private maxCompletedRunsPerInstance;
  constructor(options?: InMemoryRunRegistryOptions);
  recordRunStart(input: RecordRunStartInput): Promise<void>;
  recordRunEnd(input: RecordRunEndInput): Promise<void>;
  lookupRun(runId: string): Promise<RunPointer | null>;
  listRuns(opts?: ListRunsOpts): Promise<ListRunsResponse>;
  listInstances(opts?: ListInstancesOpts): Promise<ListInstancesResponse>;
  private pruneCompletedRunsForInstance;
}
//#endregion
//#region src/node/run-store.d.ts
declare class InMemoryRunStore implements RunStore {
  private instances;
  private maxCompletedRuns;
  private maxEventBytes;
  constructor(options?: RunStoreOptions);
  createRun(input: CreateRunInput): Promise<void>;
  endRun(input: EndRunInput): Promise<void>;
  appendEvent(runId: string, event: FlueEvent): Promise<void>;
  getEvents(runId: string, fromIndex?: number): Promise<FlueEvent[]>;
  getRun(runId: string): Promise<RunRecord | null>;
  private getInstance;
  private pruneCompletedRuns;
}
//#endregion
//#region src/session.d.ts
/** In-memory session store. Sessions persist for the lifetime of the process. */
declare class InMemorySessionStore implements SessionStore {
  private store;
  save(id: string, data: SessionData): Promise<void>;
  load(id: string): Promise<SessionData | null>;
  delete(id: string): Promise<void>;
}
//#endregion
//#region src/internal.d.ts
/**
 * Resolve `provider/model-id` to a pi-ai Model. Registered URL prefixes win
 * over pi-ai's catalog; configureProvider settings patch the resolved Model.
 */
declare function resolveModel(model: ModelConfig | undefined): Model<Api> | undefined;
//#endregion
export { type AgentHandler, type CreateContextFn, type FlueContextConfig, type FlueContextInternal, type FlueRuntime, type HandleAgentOptions, type HandleRunRouteOptions, InMemoryRunRegistry, InMemoryRunStore, InMemorySessionStore, type InstancePointer, type ListInstancesOpts, type ListInstancesResponse, type ListRunsOpts, type ListRunsResponse, type RecordRunEndInput, type RecordRunStartInput, type RunHandlerFn, type RunPointer, type RunRecord, type RunRegistry, type RunStatus, type RunStore, type RunSubscriberListener, type RunSubscriberRegistry, type StartWebhookFn, bashFactoryToSessionEnv, configureFlueRuntime, createDefaultFlueApp, createDurableRunStore, createFlueContext, createRunSubscriberRegistry, handleAgentRequest, handleRunRouteRequest, hasRegisteredProvider, parseFrontmatterFile, resolveModel };