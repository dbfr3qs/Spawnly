# Go binaries (root-level and bin/)
/agent
/agent-sidecar
/operator
/orchestrator
/registry
/sample-api
/dashboard
/bin/
*.test
vendor/

# Node
agents/*/node_modules/
agents/*/dist/
sdks/*/node_modules/
sdks/*/dist/

# E2E (Playwright) — match run artifacts wherever Playwright is invoked from
# (repo root or e2e/), not just under e2e/.
e2e/node_modules/
test-results/
playwright-report/
e2e/playwright/.auth/

# .NET build output
identityserver/bin/
identityserver/obj/
dotnet/

# IDE
identityserver/.idea/
.idea/
*.suo
*.user

# devcontainer generated
.devcontainer/devcontainer-lock.json

# Claude Code worktrees
.claude/worktrees/
.claude/

# Kubernetes / local dev
kubeconfig
*.kubeconfig

# Local secrets / env (keep .env.example tracked)
.env
.env.local
!.env.example

# Duende auto-generated dev signing keys (local runs)
identityserver/keys/
