# Expo / RN
node_modules/
.expo/
dist/
web-build/
*.log

# Local env (may hold a LAN IP / per-machine config). The committed template is
# .env.local.example.
.env
.env.local
!.env.local.example

# Native build output (iOS is built on EAS cloud macOS; Android locally)
ios/
android/

# Credentials — NEVER commit. Push keys/service accounts are mounted on the
# server (mobile-gateway), not bundled in the app.
*.p8
*.keystore
google-services.json
GoogleService-Info.plist
